import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import PressRenderer from "./PressRenderer";

<Meta title="PressRenderer" component={PressRenderer} />

# PressRenderer

This is the story for the `PressRenderer` component:

<Story name="PressRenderer">
  <PressRenderer>Positive</PressRenderer>
</Story>
